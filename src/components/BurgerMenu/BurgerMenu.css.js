import styled from "styled-components";

export const Wrapper = styled.div`
  width: 40px;
  height: 40px;

  transition: all 0.3s;
  cursor: pointer;
  :hover {
    transform: rotate(0.125turn);
  }
`;
